using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PopupUI : MonoBehaviour
{
    public ResumeBtn resumeButton_;
    public RestartLevelBtn restartLvButton_;
    public ReturnToWorldBtn returnToWorldButton_;
    public ReturnToMenuBtn returnToMenuButton_;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
